# Protein Visualization App

## Overview
The Protein Visualization App is a tool designed to help users visualize protein distrobution for single cell proteomics. It provides an intuitive interface for loading, viewing, and analyzing protein data.

## Installation
To install the Protein Visualization App, follow these steps:

1. Clone the repository:
    ```
    git clone https://github.com/yourusername/protein_visualization_app.git
    ```
2. Navigate to the project directory:
    ```
    cd protein_visualization_app
    ```
3. Install the required dependencies:
    ```
    pip install -r requirements.txt
    ```

## Usage
To start the application, run:
    ```
    python main.py
    ```
